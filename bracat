unpn() { egrep "$*.*/$" /var/db/paludis/repositories/un*/*repository| sed -e 's|/var/db/paludis/repositories/||' -e 's|/$||'};
pn(){ for x in `echo /var/db/paludis/repositories/*/packages/*/*$**`;echo `basename $x` && grep -h SUMMARY= $x/*|head -1 };
bracat(){ pn $*;echo;unpn $*; }
